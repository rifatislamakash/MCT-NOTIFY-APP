import { createClient } from '@supabase/supabase-js'; const _supabase = createClient('https://ngropmfrneaaejwocnbf.supabase.co', 'sb_publishable_re_AUBMShiJJq7tXgB3J9g_lRETY_K-'); async function check(t) { const { data, error } = await _supabase.from(t).select('*').limit(1); if(error) console.log(t, error.message); else if(data.length) console.log(t, Object.keys(data[0])); else console.log(t, 'empty'); } async function run() { await check('notices'); await check('polls'); await check('weekly_routines'); } run();
